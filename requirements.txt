# Minimal dependencies for Vercel serverless functions
# Core LLM and evaluation
langchain-core>=0.1.0
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0

# Lightweight RAGAS without heavy ML dependencies
ragas>=0.1.9

# Essential data processing (lightweight)
pandas>=2.1.4
numpy>=1.26.4

# Note: sentence-transformers and torch are too large for Vercel
# Using Google's embeddings API instead for production
